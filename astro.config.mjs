import { defineConfig } from "astro/config";

// EDIT "site" below to match your GitHub Pages URL, e.g. https://your-username.github.io
export default defineConfig({
  site: "https://your-username.github.io",
});
